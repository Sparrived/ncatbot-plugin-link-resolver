<div align="center">
<h1>✨ncatbot - LinkResolver 插件✨</h1>

一个功能强大的 NcatBot 链接解析插件，自动识别并解析消息中的各类外部链接，生成精美的预览卡片，支持多平台内容展示。


[![License](https://img.shields.io/badge/License-MIT_License-green.svg)](https://github.com/Sparrived/ncatbot-plugin-link-resolver/blob/master/LICENSE)
[![ncatbot version](https://img.shields.io/badge/ncatbot->=4.3.0-blue.svg)](https://github.com/liyihao1110/ncatbot)
[![Version](https://img.shields.io/badge/version-1.0.0-orange.svg)](https://github.com/Sparrived/ncatbot-plugin-link-resolver/releases)


</div>


---

## 🌟 功能亮点

- ✅ **自动识别** - 自动检测消息中的链接并智能解析
- ✅ **精美卡片** - 生成统一样式的预览卡片（封面 + 信息条），输出 Base64 图片
- ✅ **多平台支持** - 目前支持 Bilibili，易于扩展其他解析器
- ✅ **订阅机制** - 支持群聊和私聊订阅，灵活控制作用范围
- ✅ **手动解析** - 提供手动解析指令，可随时查看链接内容
- ✅ **短链还原** - 自动还原短链接（如 b23.tv），获取真实 URL
- ✅ **可扩展架构** - 基于 `BaseResolver` 基类，方便添加新平台解析器

## ⚙️ 配置项

配置文件位于 `data/LinkResolver/LinkResolver.yaml`

| 配置键                | 类型        | 默认值 | 说明                                                   |
| --------------------- | ----------- | ------ | ------------------------------------------------------ |
| `subscribed_groups`   | `List[str]` | `[]`   | 插件生效的群号白名单。只有在此列表中的群组才会处理链接 |
| `subscribed_privates` | `List[str]` | `[]`   | 插件生效的私聊用户白名单                               |
| `auto_parse`          | `bool`      | `true` | 是否自动解析消息中的链接                               |

**配置示例:**
```yaml
subscribed_groups:
  - '123456789'
  - '987654321'
subscribed_privates:
  - '1234567890'
auto_parse: true
```

> **提示:** 
> - 配置文件可通过 NcatBot 的统一配置机制进行覆盖
> - 建议使用 `/link subscribe` 命令动态添加订阅，避免手动修改配置后需要重启机器人
> - 未订阅的群组或私聊不会触发自动解析，也无法使用手动解析功能

## 🚀 快速开始

### 依赖要求

- Python >= 3.8
- NcatBot >= 4.3.0
- 第三方依赖：
  - `httpx` - 异步 HTTP 请求
  - `beautifulsoup4` - HTML 解析
  - `pillowmd` - Markdown 渲染

### 使用 Git

```bash
git clone https://github.com/Sparrived/ncatbot-plugin-link-resolver.git
cd ncatbot-plugin-link-resolver
pip install -r plugins/link_resolver/requirements.txt
cp -r plugins/link_resolver /path/to/your/ncatbot/plugins/
```
**⚠️ 重要：将 `resources.zip` 解压到 `data/LinkResolver/` 目录下**（解压后应该有 `data/LinkResolver/resources/` 文件夹）

> 请将 `/path/to/your/ncatbot/plugins/` 替换为机器人实际的插件目录。

### 自主下载

1. 将本插件目录置于 `plugins/link_resolver`。
2. 安装依赖：`pip install -r plugins/link_resolver/requirements.txt`
3. **⚠️ 重要：将 `resources.zip` 解压到 `data/LinkResolver/` 目录下**（解压后应该有 `data/LinkResolver/resources/` 文件夹）
4. 根据实际需要调整 `subscribed_groups` 等配置项（建议在群内使用指令调整，手动调整config需要重启机器人）。
5. 重启 NcatBot，插件将自动加载。

### 插件指令

> **注意事项:**
> - 所有指令需要先订阅才能使用（`require_subscription` 限制）
> - `subscribe` 和 `unsubscribe` 命令仅限 NcatBot 管理员用户使用（`admin_filter` 限制）
> - 自动解析功能需要订阅后才会生效
> - 以 `/` 开头的消息不会触发自动解析
> - 回复消息不会触发自动解析

| 指令                   | 参数                        | 说明                                     | 示例                                                      |
| ---------------------- | --------------------------- | ---------------------------------------- | --------------------------------------------------------- |
| `/link parse <url>`    | `url`：链接URL              | 手动解析指定链接，生成预览卡片           | `/link parse https://www.bilibili.com/video/BV1xx411c7mD` |
| `/link subscribe`      | 无                          | 订阅当前会话的链接解析功能（管理员限定） | `/link subscribe`                                         |
| `/link unsubscribe`    | 无                          | 取消当前会话的订阅（管理员限定）         | `/link unsubscribe`                                       |
| `/link help [command]` | `command`：可选，指定命令名 | 显示所有可用指令或指定命令的详细说明     | `/link help`<br>`/link help parse`                        |

## 🔍 支持的平台

### Bilibili
- **视频解析** - 支持 BV 号、AV 号链接
- **短链支持** - 自动还原 b23.tv 短链接
- **内容展示** - 展示视频标题、简介、封面、播放量、点赞数等信息
- **格式化信息** - 自动格式化播放量和点赞数（如：1.2万、345.6万）

### 扩展其他平台
插件采用模块化设计，添加新平台解析器非常简单，详见下方"开发者指南"。

## 🧠 运作逻辑

### 订阅机制
- 插件在处理消息前，会先检查群组/私聊是否在订阅列表中
- 对于未订阅的会话，插件会跳过处理，不会有任何响应
- 使用 `/link subscribe` 可快速将当前会话添加到白名单，无需重启机器人
- 群聊和私聊分别维护独立的订阅列表

### 自动解析流程
1. 接收到群聊或私聊消息
2. 检查订阅状态（未订阅则跳过）
3. 检查消息类型（跳过自身消息、命令消息、回复消息）
4. 检查 `auto_parse` 配置（如果为 false 则不自动解析）
5. 使用正则表达式提取消息中的所有 URL
6. 遍历 URL 并依次解析
7. 生成预览卡片并发送

### 解析流程
1. 使用正则表达式从消息中提取所有 HTTP/HTTPS 链接
2. 遍历已注册的解析器，查找能处理该链接的解析器
3. 调用解析器的 `parse()` 方法获取 `ParseResult`
4. 使用 `ParseResult.generate_card_image()` 生成卡片图片
5. 使用 `pillowmd` 渲染最终的 Markdown 格式结果
6. 将图片转换为 Base64 并通过 OneBot API 发送

### 短链还原
- 使用 `BaseResolver.expand_short_url()` 方法自动跟随 HTTP 重定向
- 支持 Bilibili 的 b23.tv 等短链服务
- 自动处理多次重定向，获取最终 URL

## 📊 卡片渲染

### 卡片结构
- **封面图片** - 自动缩放到目标宽度（1200px）
- **信息条** - 展示结构化元数据（播放量、点赞数等）
- **统一边框** - 1px 灰色边框，提升视觉效果
- **透明背景** - 方便与其他元素组合

### Markdown 渲染
- 使用 `pillowmd` 库进行高质量 Markdown 渲染
- 支持自定义样式（mdstyle 文件夹）
- 头像、图表、文字完美融合
- 输出 PNG 格式图片，便于分享

### 性能优化
- 临时文件自动清理，避免磁盘占用
- 图片缓存机制，减少重复渲染
- 异步处理，不阻塞消息处理流程

## 🛠️ 开发者指南

### 添加新的解析器

解析器位于 `plugins/link_resolver/resolvers/`。新解析器应继承自 `BaseResolver` 并实现必要的方法：

- 提供 `can_handle(self, url: str) -> bool` 方法，用于判断是否可解析该 URL
- 提供 `async def parse(self, url: str) -> ParseResult` 异步方法，返回 `ParseResult` 实例
- 使用 `@register_resolver` 装饰器注册解析器

**最小示例：**

```python
from .base_resolver import BaseResolver, ParseResult, register_resolver

@register_resolver
class ExampleResolver(BaseResolver):
    """示例平台解析器"""
    
    def can_handle(self, url: str) -> bool:
        """判断是否可处理该 URL"""
        return 'example.com' in url

    async def parse(self, url: str) -> ParseResult:
        """解析链接并返回结果"""
        # 1. 获取页面内容
        html = await self.fetch_page(url)
        
        # 2. 解析内容（使用 BeautifulSoup 等）
        title = "示例标题"
        description = "示例描述"
        banner_url = "https://example.com/image.jpg"
        
        # 3. 下载封面图片
        banner_b64 = await self.download_image_as_b64(banner_url)
        
        # 4. 返回 ParseResult
        return ParseResult(
            title=title,
            description=description,
            banner_b64=banner_b64,
            url=url,
            platform='Example',
            metadata={
                "info_line": {
                    "播放": "1.2万",
                    "点赞": "3456"
                }
            }
        )
```

### ParseResult 数据结构

`ParseResult` 包含以下字段：

- `title`：标题（字符串）
- `description`：文本描述
- `banner_b64`：封面图片 base64（可选）
- `url`：原始链接
- `platform`：平台名称
- `pre_init_images`：预生成的局部图片（例如 info 条）的 base64 列表
- `metadata`：结构化元数据字典
- `card_color`：卡片主色（RGB 三元组，可选）

`ParseResult` 自带 `generate_card_image()` 方法，负责将封面和信息条合成为最终卡片。

### 使用 BaseResolver 工具方法

`BaseResolver` 提供了一些实用方法：

```python
# 发送 HTTP 请求获取页面内容
html = await self.fetch_page(url)

# 下载图片并转换为 base64
image_b64 = await self.download_image_as_b64(image_url)

# 还原短链接
real_url = await self.expand_short_url(short_url)
```

## 🪵 日志与排错

插件使用 NcatBot 的统一日志系统，所有操作都会记录详细日志。

### 查看日志
```bash
# 日志文件位置
logs/bot.log.YYYY_MM_DD

# 筛选 LinkResolver 相关日志
grep "LinkResolver" logs/bot.log.2025_11_17
```

### 常见问题

**Q: 为什么指令没有响应？**
- 检查当前群/私聊是否已订阅（使用 `/link subscribe`）
- 确认发送 `/link subscribe` 指令的用户是否在 NcatBot 的管理员列表中
- 查看日志确认是否有错误信息

**Q: 自动解析不工作？**
- 检查 `auto_parse` 配置是否为 `true`
- 确认当前会话已订阅
- 确认消息不是以 `/` 开头的命令
- 确认消息不是回复消息

**Q: 解析失败并提示网络错误？**
- 检查服务器能否访问目标站点（防火墙/网络限制）
- 确认 `httpx` 请求是否被目标平台拒绝（User-Agent/Referer）
- 查看日志中的详细错误信息

**Q: 卡片图片显示异常或字体乱码？**
- 确保系统安装了常用中文字体（如 `msyh.ttc`）
- 确认 `data/LinkResolver/resources/mdstyle` 目录是否完整
- 若字体缺失，pillow 会回退到默认字体，可能造成排版差异

**Q: 短链无法还原？**
- 检查网络连接，确保能访问短链服务
- 查看日志中是否有 "timeout" 或其他错误
- 某些短链服务可能有防爬策略，需在请求头中模拟浏览器

**Q: 如何测试解析器？**
- 直接运行解析器脚本进行验证（例如：`python plugins/link_resolver/resolvers/bilibili.py`）
- 查看控制台输出和生成的图片
- 使用 `/link parse` 命令进行实际测试

## 🤝 贡献

欢迎通过 Issue 或 Pull Request 分享改进建议、提交补丁！

### 贡献指南
1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码规范
- 遵循 PEP 8 Python 代码风格
- 添加必要的注释和文档字符串
- 确保代码通过基本功能验证
- 新增解析器需要继承 `BaseResolver` 类并使用 `@register_resolver` 装饰器

### 添加新平台支持
如果想添加新的平台解析器，请参考上方"开发者指南"，并确保：
- 解析器能正确处理该平台的链接
- 提供完整的错误处理
- 生成的卡片符合统一样式
- 在 PR 中说明支持的平台和功能

## 🙏 致谢

感谢以下项目和贡献者：

- [NcatBot](https://github.com/liyihao1110/ncatbot) - 提供稳定易用的 OneBot11 Python SDK
- [httpx](https://www.python-httpx.org/) - 现代化的 HTTP 客户端
- [BeautifulSoup](https://www.crummy.com/software/BeautifulSoup/) - 强大的 HTML 解析库
- [pillowmd](https://github.com/Monody-S/CustomMarkdownImage) - Markdown 图像渲染库
- 社区测试者与维护者 - 提交 Issue、Pull Request 以及改进建议

## 📄 许可证

本项目采用 [MIT License](LICENSE) 开源协议。

---

<div align="center">

如果本插件帮助到了你，欢迎为项目点亮 ⭐ Star！

[报告问题](https://github.com/Sparrived/ncatbot-plugin-link-resolver/issues) · [功能建议](https://github.com/Sparrived/ncatbot-plugin-link-resolver/issues) · [查看发布](https://github.com/Sparrived/ncatbot-plugin-link-resolver/releases)

</div>
